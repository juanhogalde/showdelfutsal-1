const equiposPorDefecto = {
  equipos: [],
  equipo: {},
};

const storeEquipos = (state = equiposPorDefecto, accion) => {
  switch (accion.type) {
    default:
      return state;
  }
};
export default storeEquipos;
