const estadiosPorDefecto = {
  estadios: [],
};

const storeEstadios = (state = estadiosPorDefecto, accion) => {
  switch (accion.type) {
    default:
      return state;
  }
};
export default storeEstadios;
