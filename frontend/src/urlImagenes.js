import {api} from '../src/api';
export const urlImagenes = `http://${api}:4000`;
export const urlStaticImages = `http://futsal.lowa.com.ar/static/media/`; // en local http://localhost/Static/Escudos/
